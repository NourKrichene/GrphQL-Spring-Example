schema {
  query: Query
}

type Query {
     city(id : ID) : City
}

type City {
     id : ID !
     name : String
     population : Int
}

type Mutation {
    saveCity(name: String, population: Int) : City
}