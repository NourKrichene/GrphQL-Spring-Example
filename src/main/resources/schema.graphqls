type City {
     id : ID !
     name : String
     population : Int
}

type Query {
     city(id : ID) : City
     findAll : [City]
     findByName(name : String) : [City]
}

type Mutation {
    addCity(name: String, population: Int) : City
    deleteCity(id : ID) : String
}

    